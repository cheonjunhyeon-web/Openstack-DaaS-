# Controller Node - Keystone 설정
# OpenStack 인증(Authentication) 및 인가(Authorization) 전담 서비스
# 모든 서비스 요청의 첫 번째 관문

[database]
# MariaDB 연결 (관리망 IP 기준)
connection = mysql+pymysql://keystone:keystone@10.0.0.10/keystone

[token]
# Fernet 토큰 방식: DB 조회 없이 토큰 자체에 정보 포함 → 성능 향상
provider = fernet
