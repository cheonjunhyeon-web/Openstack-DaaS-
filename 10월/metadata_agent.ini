# Controller Node - 메타데이터 에이전트 설정
# 인스턴스가 cloud-init 등을 통해 메타데이터(IP, 호스트명 등)를 조회할 수 있도록 지원

[DEFAULT]
nova_metadata_host = 10.0.0.10
# shared secret은 nova.conf의 metadata_proxy_shared_secret과 동일하게 설정
metadata_proxy_shared_secret = <shared_secret>
